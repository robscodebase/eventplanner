version: '3'

services:
    db:
        image: mysql
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: insecure

    event-planner:
        build: .
        ports: 
            - "8081:8081"
        depends_on:
            - db


